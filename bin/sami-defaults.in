#!/usr/bin/env sh

@GENERATED_WARNING@

# Default values for environment variables used by several scripts
# of the korowai/sami docker image.

@SAMI_ENV_DEFAULTS@
DEFAULT_SAMI_BUILD_LOCK='/tmp/sami-build.lock'
